import Placeholder from "@/components/placeholder";

export default function Page() {
  return (
    <Placeholder
      heading="Weekly Drop Management"
      body="Select items for drop, schedule locked to Friday 6PM, preview, publish."
      cta="Publish Drop"
    />
  );
}
