import Placeholder from "@/components/placeholder";

export default function Page() {
  return (
    <Placeholder
      heading="Settings"
      body="Outlet locations, shipping rates, return window, user roles and permissions."
      cta=""
    />
  );
}
