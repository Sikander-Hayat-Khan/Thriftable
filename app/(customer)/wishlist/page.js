import Placeholder from "@/components/placeholder";

export default function Page() {
  return (
    <Placeholder
      heading="Saved Items"
      body="Items move fast on Weekly Drop days. Saved items aren't reserved."
      cta="Move to Cart"
    />
  );
}
