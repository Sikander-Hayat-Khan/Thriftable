import Placeholder from "@/components/placeholder";

export default function Page() {
  return (
    <Placeholder
      heading="Past Orders"
      body="Every order shown with status, items, and outlet or shipping method."
      cta="Start a Return"
    />
  );
}
